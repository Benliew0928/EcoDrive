# EcoDrive+ Shared UI Package

Shared UI components and tokens can live here once the dashboard and simulator both have selected visual systems.

For now the cockpit dashboard keeps its static shell inside `apps/dashboard`.

